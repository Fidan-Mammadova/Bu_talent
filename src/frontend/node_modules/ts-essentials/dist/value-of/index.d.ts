import { AnyArray } from "ts-essentials/dist/any-array";
import { AnyFunction } from "ts-essentials/dist/any-function";
import { Primitive } from "ts-essentials/dist/primitive";
export declare type ValueOf<Type> = Type extends Primitive ? Type : Type extends AnyArray ? Type[number] : Type extends AnyFunction ? ReturnType<Type> : Type[keyof Type];
