import { EmbeddedValidationRule } from "quasar/dist/types/api/validation.js";

interface Patterns {
  /**
   * Test against particular patterns.
   */
  testPattern: Record<EmbeddedValidationRule, (value: any) => boolean>;
}

export declare const patterns: Patterns;
