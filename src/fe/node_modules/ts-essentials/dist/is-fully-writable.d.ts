import { IsEqualConsideringWritability } from "ts-essentials/dist/is-equal-considering-writability";
import { Writable } from "ts-essentials/dist/writable";
export declare type IsFullyWritable<Type extends object> = IsEqualConsideringWritability<
  {
    [Key in keyof Type]: Type[Key];
  },
  Writable<{
    [Key in keyof Type]: Type[Key];
  }>
>;
